export const HeaderTemplates = ({ title }) => {
  return `
      <div></div>
      <h1>${title}</h1>
      <span id="modal-open-button" class="icon material-symbols-outlined">
        leaderboard
      </span>
  `;
};
